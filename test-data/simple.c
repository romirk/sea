int x;

char bar(int c) {
    int y;
    for(expr;;) {
        if (expr) break;
        else do 
            char c;
        while (expr);
        goto label;
    }
    return expr;
}

typedef char c;